// Override stray C:\postcss.config.mjs – no PostCSS plugins needed
// Tailwind CSS v4 is handled via @tailwindcss/vite plugin in vite.config.js
const config = {
  plugins: {},
}

export default config
